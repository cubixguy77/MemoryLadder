<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--suppress AndroidUnknownAttribute -->
    <data class="DatesFragmentBinding">
        <import type="android.view.View" />
        <variable
            name="viewModel"
            type="com.memoryladder.taketest.dates.ui.viewmodel.DatesViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/root_dates"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:animateLayoutChanges="true"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/timerContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/text_date_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start|center_vertical"
                android:text="@{@string/dateCount(viewModel.dateCount)}"
                android:textSize="@dimen/page_selector.text.size"
                tools:text="XX Dates" />

            <include
                layout="@layout/viewgroup_timer">
            </include>
        </FrameLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/date_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="16dp"
            android:layout_marginLeft="4dp"
            android:layout_marginRight="4dp"
            android:layout_marginBottom="16dp"
            tools:listitem="@layout/list_item_dates"
            />
    </LinearLayout>
</layout>